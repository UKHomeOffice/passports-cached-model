{
  "name": "hmpo-cached-model",
  "version": "2.1.0",
  "description": "Cached polling model",
  "main": "lib/index.js",
  "scripts": {
    "test": "npm run lint && npm run unit && npm run check-coverage && npm audit",
    "lint": "eslint .",
    "unit": "nyc --reporter=lcov --reporter=text-summary _mocha test/ --recursive",
    "check-coverage": "nyc check-coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/UKHomeOffice/passports-cached-model.git"
  },
  "author": "HMPO",
  "license": "MIT",
  "engines": {
    "node": ">=8"
  },
  "bugs": {
    "url": "https://github.com/UKHomeOffice/passports-cached-model/issues"
  },
  "homepage": "https://github.com/UKHomeOffice/passports-cached-model",
  "dependencies": {
    "async": "^3.1.1",
    "debug": "^4.1.1",
    "hmpo-model": "^2.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^6.8.0",
    "mocha": "^7.0.1",
    "nyc": "^15.0.0",
    "sinon": "^8.1.1",
    "sinon-chai": "^3.4.0"
  },
  "nyc": {
    "all": true,
    "exclude": [
      "coverage/**",
      "test/**"
    ],
    "lines": 100,
    "branches": 100,
    "functions": 100,
    "statements": 100
  }
}
